/* pseudocode
Need to create 3 variables for step 1
Each of those variables needs to perform one math oeration 
that equals 1 of the 3 numbers
that needs to then be displayed to the user
*/

let firstNumber = 5 * 2;

let secondNumber = 80 / 2;

let thirdNumber = 30 + 10 - 1

let displayString = `You have received this message because 
you have been chosen to open an important vault. 
Here is the secret combination: ${firstNumber} - ${secondNumber} - ${thirdNumber}`

alert (displayString)